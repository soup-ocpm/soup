/**
 * Graph type enum
 * @version 2.0
 * @since 1.0.0
 * @author Alessio Giacché
 */
export enum GraphType {
  // The standard graph
  Standard = 1,

  // The aggregate graph
  Aggregate = 2,

  // The filtered graph
  Filtered = 3
}
