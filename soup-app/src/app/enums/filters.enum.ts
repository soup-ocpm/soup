/**
 * Filters enum
 * @version 1.0
 */
export enum FiltersEnu {
  // Timestamp filter
  TimestampFilter = 0,

  // Performance filter
  PerformanceFilter = 1,

  // Include activities filter
  IncludeActivityFilter = 2,

  // Eclude activities filter
  ExcludeActivityFilter = 3,

  // Frequence filter
  FrequenceFilter = 4,

  // Variation filter
  VariationFilter = 5
}
