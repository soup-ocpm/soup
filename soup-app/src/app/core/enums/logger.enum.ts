/**
 * Logger type enum
 * @version 1.0
 */
export enum LogLevel {
  // Debug log level (all logs)
  Debug = 1,

  // Info log level (only info)
  Info = 2,

  // Warn log level (only warning)
  Warn = 3,

  // Error log level (only errors)
  Error = 4
}
